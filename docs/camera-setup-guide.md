# Camera Setup Guide

## RTSP
Use URL: `******camera-ip:554/stream`.

## ONVIF
Enable ONVIF in camera settings and ensure credentials are correct.

## USB/Laptop Cameras
Use numeric source index (`0`, `1`) for OpenCV capture.
